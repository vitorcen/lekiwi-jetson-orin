---
name: vad-asr-tuning-results
description: VAD/ASR 调参已完成的结论——silero 完胜 fsmn、增益必须为 0、四个参数可统一只有 min_speech 各调、Fun-ASR 从噪声里编话挡不住
metadata:
  type: project
---

2026-07-25 完成的 VAD/ASR 调参实验。方法：Mac 用 **edge-tts 六个音色**合成固定语料
播放 → 板子麦克风录 → **离线**扫 480 组参数（三引擎 × 同一份录音）→ Fun-ASR 验证。
工具在 `scripts/vad_corpus.py` / `vad_record.py` / `vad_sweep.py` / `vad_pick.py` /
`vad_asr.py` / `mic_compare.py`，录音落在板上 `/tmp/vadtune/`。

**离线扫参是这个实验能成立的前提**：在活麦上顺序 A/B 比的是房间不是参数。
录一次、离线重放，同一份字节喂给每一组配置。

**语料必须多音色且可复现**，别用 Matcha——它是流匹配模型每次重画（同一个「停」
0.29/0.39/0.41 秒），而且自己就念不清（数字语料 13/14 CER 0.143，edge-tts 是
83/84 CER 0.012）。详见 [[measurement-validity]] 第 12 条。

## 已落盘的配置（`~/.config/lekiwi/config.json`）

```
vad:   engine=silero  threshold=0.30  min_speech_s=0.10  min_silence_s=0.40  pre_roll_s=0.60
audio: gain_db=0.0
```

改动前是 `fsmn / min_speech 0.10 / pre_roll 0.90 / gain +15`。

## 三引擎实测（14 句 6 音色,近 +19.4dB 与远 +12.9dB 两档,5 分钟空房间）

| 引擎 | 最优参数 | near | far | 空闲误判 | 漏/并 |
|---|---|---|---|---|---|
| **silero** | 0.30 / 0.10 / 0.40 / 0.60 / gain 0 | 13/14 | 14/14 | **4.8%** | 1 / 0 |
| ten | 0.50 / 0.10 / 0.40 / 0.60 / gain +6 | 13/14 | 13/14 | 7.5% | 2 / 1 |
| fsmn | msp 0.60 / pre 0.60 / gain 0 | 10/14 | 10/14 | 9.8% | 8 / 6 |

**fsmn 不可用于连续短指令**：48 组里没有一组能把间隔 0.8–1.2 秒的两条指令切开
（漏 8 并 6）。粘连发生在 FSMN 自己的状态机里，它只暴露 `min_speech_s` 和
`pre_roll_s` 两个旋钮，外面调不动。

端到端（Fun-ASR 转写，oracle 上限是 near 14/14 CER 0.000 / far 11/14 CER 0.131）：

| | near 全对 | near CER | far 全对 | far CER | 空房间起轮 |
|---|---|---|---|---|---|
| **silero 0.30** | 11/14 | **0.049** | 12/14 | **0.036** | **1.0 次/分** |
| 旧生产 fsmn/+15 | 5/14 | 1.296 | 5/14 | 1.031 | 3.4 次/分 |

**阈值的最优点会随语料的音色多样性移动。** 单音色 Matcha 语料上 0.70 最好
（14/14+14/14）；换成 6 音色 edge-tts，0.70 掉到 12/14+10/14 而 0.30 是 13/14+14/14
——高阈值把偏轻偏柔的嗓子漏掉了。误触发那侧代价几乎没有：0.30 比 0.70 每 5 分钟
多切 1 段，但那段解码为空被下游挡掉，**真正起轮反而更少**（1.0 vs 1.2 次/分）。
**用单一合成嗓子调阈值必然调偏。**

## 哪些参数能统一（三引擎强制同值的代价）

- `pre_roll_s = 0.6` —— 三个引擎最优值**完全一致**，零代价（两份语料上都成立）
- `min_silence_s = 0.4` —— silero/ten 一致（fsmn 不适用），两份语料上都成立
- `gain_db = 0` —— 统一，代价只落在 ten 且很小
- `threshold` —— silero 要 0.3、ten 要 0.5，强行统一到 0.5 让 silero 退化 1 分。
  **这一项对语料的音色多样性敏感，别照抄，换环境要重测**
- `min_speech_s` —— **必须各调**：fsmn 要 0.6，silero/ten 要 0.1；强行统一退化 10 分

## 三条反直觉的实测结论

**增益是纯亏损项，不是可调项。** MCP01 麦克风自带 AGC——数字上差 12 dB 的两次播放，
录进来峰值都是 -9.3 dBFS。所以任何正增益都必然削顶：+15 dB 实测削掉 **14.8%** 的语音样本。
其他参数不变、只把增益 0→+15，near 召回就从 14/14 掉到 12/14。

**`classify_segment` 这道下游闸门形同虚设。** funasr 从纯房间噪声里编出通顺整句
（"武汉和广东也剧烈的战了一下手"、日语 "さて、考えそう"）。silero/ten 的噪声段
**100%** 被判 ACCEPTED。VAD 误触发基本 1:1 变成机器人自言自语，中间没有任何东西挡。
所以 VAD 误触发必须压到接近 0——`<ignore>` 哨兵已经是一整轮 LLM 之后的事了。

**板子麦克风在词首爆破音上确实吃亏,而且吃的正是「停」。** 双麦同录、同一个
Fun-ASR、oracle 切分,并把 Mac 那份**掺入它自己的底噪**降到板子麦的
+12.1dB 辅音带信噪比再比（不然比的是距离不是麦克风）：

| | 1-3.4k SNR | 逐字全对 | CER |
|---|---|---|---|
| 数字语料 | — | 14/14 | 0.000 |
| Mac 麦 | +27.1dB | 14/14 | 0.000 |
| **Mac 麦降到同信噪比** | +12.1dB | **14/14** | **0.000** |
| **板子麦** | +12.1dB | **12/14** | **0.155** |

错的两句是**同一个现象**：词首的「停」（tíng,送气清塞音）。
「停」→「一瓶」,「停下来别动了」→「接下来别动了」。其余 12 句含长句全对。

**机制假说**：daemon 注释里写明 **MCP01 带硬件降噪门**（"静时输出近似静音,
ALSA 增益调了没用"）。降噪门从静音开启要几毫秒,正好吃掉词首爆破音的起音。
这解释了为什么 `pre_roll` 救不了——被吃掉的采样在硬件层就没了,前置再多只是多拿静音。

**边界**：掺噪降的是信噪比,没有复现距离带来的混响和高频衰减,
所以这 2 句的差距里有一部分该记在房间而不是麦克风上。

**注意这条修正过**：早先用 Matcha 语料得出「板子麦没问题」,是因为那份语料自己
就念不清(往前走一点点→往前存做点点)、还每次重画,把差异盖住了。
换成可复现的多音色 edge-tts 语料后差异是实的。

**实际影响**：「停」是这套指令里唯一的安全指令,恰好是板子音频链路最不擅长的那个。

## 两个工具坑

**MCP01 是免提一体机,喇叭和麦在同一个 USB 设备里。** 板子放自己的音、用自己的麦录,
录到的东西被设备自己的回声抵消压到**连包络互相关都锁不住**(z=1.8)。
所以「板子放音→板子麦」这条路量的是设备 AEC,不是麦克风。对打断检测也有影响:
机器人自己说话时,麦听到的是被抑制过的版本。

**ffmpeg + avfoundation 在这台 Mac 上稳定丢 23% 采样**——20.5 秒墙钟只录到 15.9 秒,
原生 48kHz 不做重采样也一样,可复现。录出来不只是短,**时间轴是拼接的**,
包络对齐锁不住。改用 `sounddevice`(PortAudio)后是 0.992。Mac 端录音一律别用 ffmpeg。

## ⚠ silero 会适应底噪:冷启动测 VAD 全是假的(2026-07-28,最重要的一条)

**silero 的 RNN 会适应背景噪声,而离线扫参每个变体都是新建实例 = 冷启动,
生产里不存在。** 实测同一个实例、同一份低电平录音连喂三遍:**8 段 → 0 段 → 1 段**;
`reset()` 能完全救回(→8 段)。预灌底噪只要 **2 秒**就掉一半,**5 秒**就只剩 1 段。
高电平(段峰 −17~−23 dBFS)不受影响,一直 10 段 —— 所以这是个**只在低信噪比下发作**
的效应,也正是"正常音量说话老是漏句、大声就好了"的真正机制。

**别指望定期 reset 救它**:实测每 2/5/10 秒在静默期 reset,低电平也只从 1 段回到
2 段 —— 两句之间那 2 秒底噪够它重新适应完。(好消息:150 秒空房间录音上,
任何 reset 频率都是 **0 误触发**,所以代价那一侧是干净的。)

**试过的东西没一个能救**(全部同一批字节、真底噪预灌、板上在跑的 qwen3 解码):

| 手段 | 结果 |
|---|---|
| 定期 reset(每 2/5/10s 静默期) | 低电平 1 段 → 2 段。两句之间的底噪够它重新适应完 |
| **150Hz 高通** | **完全无效**。新录音各播 3 遍 n=30:音量69 有无高通都是 30 段/25 句全对;音量50 都是 **0 段** |
| 阈值 0.20 / 0.15 | 段更长 → 并句,更差 |
| TEN VAD | 1~2 段,且空房 60s 误检 3 次 |

**唯一的判据是电平**:段峰值约 **−22 dBFS(系统音量69)** 时 30/30 段、25/30 逐字全对;
再低一档(**−26 dBFS,音量50**)**直接 0 段**,任何配置都救不回来。这是个断崖,不是斜坡。
所以"听不清"的正解是**把电平找回来**(距离/朝向/音箱/换麦),不是调软件。
**换成 MCP01 之后这整节的病基本消失**(它底噪低 20dB,RNN 没得适应)——
数字和两只麦的对比见 [[voice-mic-level-cliff]]。

**中途翻过两次车,方法论教训比结论值钱**:
1. **冷启动数字全是假的**(每个变体 new 一个实例)。加上预灌后排序整个变了。
2. **预灌用「2 秒噪声循环铺开」也是假的** —— 周期信号不是噪声。要用真录音。
3. **单份录音上的胜负不可信**:rec50 上高通 1→9 段,换新录音 0→0。n=1 的结论
   在这条链路上纯属抽签,要么多录几遍、要么别下结论。
见 [[measurement-validity]]。

## 正常音量下瓶颈不是 VAD 而是解码(2026-07-28,一份录音七个变体,**冷启动口径,已被上一节推翻**)

用户「系统音量 100 太刺耳,69 才是正常音量」而 69 档漏句 —— 于是在**一份 69 档录音**
(段峰值 −17~−23 dBFS,底噪 −38)上离线比七个 VAD 变体。结论是一串负结果:

| 变体 | 段数(应 10) | 备注 |
|---|---|---|
| **silero 0.30 原始(现状)** | **10** | 七个里最好 |
| silero 0.30 + 150Hz 高通 | 9 | 并了两句 |
| silero 0.30 + 高通 + 8dB | 8 | 更差 |
| silero **0.20** | 8 | 阈值调低 = 段更长 = 并句,还漏了一句 |
| silero 0.20 + 高通 | 7 | 最差组合 |
| **ten** 0.30 | **2** | 几乎全并成一段,「更大的 VAD」在这里是反效果 |

**别再调 VAD 参数了**,也别上高通:实测 150Hz 高通只把信噪比抬 **+4.1 dB**
(不是 [[voice-mic-br21-noise-usb]] 从底噪频谱推的 +8 dB —— 语音自己也有 100Hz 以下
能量,一起被砍),而且代价是 VAD 开始并句。**从频谱推出来的收益,要在真实录音上兑现
过才算数。**

解码这一侧,同一批 10 段(ASR 无内部状态,这组数不受上面那条影响):

| 引擎 | 全对 | 解码 10 段 | 真错的 |
|---|---|---|---|
| sensevoice | 7/10 | 1.5s | 小飞车、退后办理(另一条是 ITN 把「九十」写成 90,不算错) |
| funasr | 9/10 | 6.3s | 小飞车 |
| **qwen3(板上实际在用)** | **9/10** | 13.9s | 最后半米 |

**换引擎买不到准确率**,qwen3 已经是准的那个;funasr 同分而快 2.2 倍,只买速度。

**踩坑**:一开始我以为在用 sensevoice,拿它当基线得出「换引擎免费涨 2/10」——错的。
**活跃 ASR 引擎只在 `/config.applied.asr` 里,`/health` 根本不报**(只有
`asr_loaded` 布尔和 `engines.tts_engine`),很容易假设错。查引擎看 `/config`。
另:`ephemeral:true` 的引擎覆盖在**关转写台时会还原**,长实验中途别关台。

## 根因:长跑的 silero 实例会退化,静默期刷新即治(2026-07-28 定案)

**症状**:「一长句接一短句,短句丢」「句子前面两个字丢」。

**根因**(用 daemon 当时真正听到的那份 PCM 做的反事实,不是另录一份):同一批字节,
**新建 VAD 切出完整的「帮我看一下桌子上面放着什么」,喂过 90 秒真实语音历史的实例
只切出「桌子上面放着什么」**。音频、增益、ASR 全部排除 —— 是长跑实例自己触发变晚:
句首没进段;短句(0.5s)在它反应过来之前就说完了,整句消失。

**修法**:静默满 `VAD_REFRESH_IDLE_S`(默认 2.0 秒**音频时间**,不是墙钟)且没有在途段时,
`vad.refresh()`。效果:
- 离线配对(4 份故障 PCM × 6 句,同一批字节):**18/24 → 20/24**
- 实时(同一套跑法,修复前 4 遍遍遍有丢):**4 遍 24/24 全清**

**三个不能改的细节**:
1. **计时用音频时间**(喂进去的采样数/16000)。ASR 推理和 VAD 在同一个进程,墙钟间隔
   里混着调度延迟,对不齐波形。
2. **别刷太勤**:静默 0.5s 刷一次实测**更差**(17/24)—— 刷新会打断正在累积的起音判定。
   2.0s 和 1.0s 都是 20/24。
3. `refresh()` 保留回看环、`reset()` 会清掉它。两者在 n=24 上**打成平手**,保留环是
   理论上的正确(免得刚 reset 完就有人说话丢句首),不是实测收益。

**顺带**:定期 refresh 会把 sherpa 内部那个 int32 采样计数清零,躲开它 16k 下
约 37 小时的溢出(codex 评审指出,未实测)。

相关：[[measurement-validity]] [[voice-asr-engines]] [[omni-brain-two-stage]]
[[voice-mic-level-cliff]]
