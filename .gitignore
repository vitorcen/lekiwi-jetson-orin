# L4T downloads (2.6GB) — re-fetch from developer.nvidia.com, see docs/
download/

# Extracted BSP working tree (tens of GB, root-owned rootfs)
Linux_for_Tegra/

# flash run log
flash.log
__pycache__/
temp/
.DS_Store

# voice frontend: venv + downloaded ASR/TTS/VAD models (re-fetch via voice/setup.sh)
voice/.venv/
voice/models/
voice/token

# secrets: never commit env files anywhere (Hermes .env lives on-box only)
**/.env
